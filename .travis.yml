language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-famous-angular' ]; then cd .. && eval "mv $currentfolder generator-famous-angular" && cd generator-famous-angular; fi

